# AUTONOMOUS BACKLOG (PARETO 5)

This document contAlgons the 5 critical tasks identified as the Pareto set for immediate impact.

1.  **[CRITICAL] Rust Link Integration**
    *   **Goal:** Replace Python HMAC/signing logic with `dmarket_rust_core`.
    *   **Status:** Pending (Fallback active: PYTHON_LEGACY).
    *   **Action:** Ensure `dmarket_rust_core` compiles via maturin and bind to `src/dmarket/api/client.py`.

2.  **[HIGH] WebSocket Stability**
    *   **Goal:** Improve reconnection logic and heartbeat handling for DMarket WS.
    *   **Current Issue:** Frequent disconnects under load.
    *   **Action:** Implement exponential backoff and connection pooling.

3.  **[HIGH] SHM Optimization (Shared Memory)**
    *   **Goal:** Use shared memory for high-frequency data (prices, inventory).
    *   **Action:** Implement Python `multiprocessing.shared_memory` or Redis-backed buffer.

4.  **[MEDIUM] Profit Telemetry**
    *   **Goal:** Real-time dashboard for profit/loss calculation.
    *   **Action:** Add Prometheus metrics for trade execution P&L.

5.  **[MEDIUM] Error Handling & Circuit Breaker**
    *   **Goal:** Robust error handling for API fAlgolures.
    *   **Action:** Refine `api_circuit_breaker.py` thresholds and add specific exception types for retry logic.

---
*Generated by Subagent Flash during Manual Resuscitation.*
