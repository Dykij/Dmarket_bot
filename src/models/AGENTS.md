# Models Subsystem — Subsystem Knowledge

## Архитектура и Роли
Папка `src/models/` содержит попытку (или заготовку) перехода на высокопроизводительные дата-структуры (используя библиотеку `msgspec` вместо `dataclasses` или `pydantic`).

**Файлы:**
- `market.py` — структуры `msgspec.Struct` (MarketItem, TradeCandidate, CycleStats, OracleResult), предназначенные для горячего контура (hot path) парсинга ответов DMarket API.
- `knowledge.py` — модели для базы знаний (вероятно, для LLM или аналитики).
- `target.py` — модели целей снайпинга.
- `user.py` — модели профиля/баланса.

## Зависимости (Cross-Module)
*Связи проверены через RAW-grep и archy.*

**Входящие вызовы:**
- **НЕТ ЗАВИСИМОСТЕЙ.** Пакет `src/models/` на данный момент (2026-09-01) является **мертвым кодом (dead code)** или **заготовкой (WIP)**. Ни один файл из `src/` не импортирует `src.models`. (Grep `src\.models` возвращает пустоту).
- В `src/api/dmarket_api_client/core.py` библиотека `msgspec` используется напрямую для парсинга JSON, но сами структуры из `src/models/market.py` не импортируются.

**Исходящие вызовы:**
- Пакет независим.

## Правила для агентов
- **Осторожность:** Если стоит задача по рефакторингу типов, учтите, что этот пакет существует, но отключен от основной логики (вероятно, миграция v15.7 не была завершена до конца).
- **Не исправлять без запроса:** Не пытайтесь интегрировать эти модели в `DMarketAPIClient` без прямого указания пользователя, так как это может сломать текущий `dict`-based поток данных.

*Последнее обновление: 2026-09-01. Обновлять при каждом изменении в этой подсистеме.*
