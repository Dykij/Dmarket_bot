"""
skinport_oracle.py — DEPRECATED / REMOVED.

Skinport is not available in Ukraine. This module is kept as a stub
to avoid import errors if any legacy code references it.

DO NOT USE — the oracle is non-functional.
"""
